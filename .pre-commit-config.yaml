repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-merge-conflict

# Temporarily disabled detect-secrets due to missing dependencies
  # - repo: https://github.com/Yelp/detect-secrets
  #   rev: v1.5.0
  #   hooks:
  #     - id: detect-secrets
  #       args: ['--baseline', '.secrets.baseline']
  #       exclude: ^(flake\.lock|\.git/)

# Temporarily disabled ggshield due to missing API key
  # - repo: https://github.com/gitguardian/ggshield
  #   rev: v1.30.0
  #   hooks:
  #     - id: ggshield
  #       language: python
  #       stages: [pre-commit]

# Temporarily disabled gitleaks due to false positives on baseline file
  # - repo: https://github.com/gitleaks/gitleaks
  #   rev: v8.21.2
  #   hooks:
  #     - id: gitleaks

  - repo: local
    hooks:
      - id: check-private-keys
        name: Check for private keys
        entry: bash -c 'if grep -r --include="*.pem" --include="*.key" --include="*.p12" --include="*.pfx" "BEGIN.*PRIVATE KEY\|BEGIN RSA PRIVATE KEY\|BEGIN DSA PRIVATE KEY\|BEGIN EC PRIVATE KEY" .; then echo "Private key detected!"; exit 1; fi'
        language: system
        pass_filenames: false

# Temporarily disabled sensitive patterns check due to false positives
      # - id: check-sensitive-patterns
      #   name: Check for sensitive patterns
      #   entry: bash -c 'if grep -r --exclude-dir=.git --exclude="*.md" -i "password\s*=\|api_key\s*=\|secret\s*=\|token\s*=\|credential" .; then echo "Potential credential detected!"; exit 1; fi'
      #   language: system
      #   pass_filenames: false

# Temporarily disabled nix validation due to verbose output
      # - id: nix-file-validation
      #   name: Validate Nix files
      #   entry: nix-instantiate --parse
      #   language: system
      #   files: '\.nix$'
